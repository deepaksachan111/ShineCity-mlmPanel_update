<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.deepaksachan.shinecity_mlmpanel" >

    <!-- Internet Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Network State Permissions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".DefaultScreenActivity"
            android:label="@string/app_name"
            android:theme="@style/splashScreenTheme" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme" >
        </activity>
        <activity
            android:name=".HomeActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="Home"
            android:theme="@style/CustomActivityTheme" >
        </activity>
        <activity
            android:name=".EPinRegistrationActivity"
            android:label="@string/title_activity_epin_registration"
            android:theme="@style/CustomActivityTheme" >
        </activity>
        <activity
            android:name=".EPinRegisteredSuccess"
            android:label="@string/title_activity_epin_registered_success"
            android:theme="@style/CustomActivityTheme" >
        </activity>
        <activity
            android:name=".OTPPasswordChangeActivity"
            android:label="@string/title_activity_otppassword_change"
            android:theme="@style/CustomActivityTheme">
        </activity>
    </application>

</manifest>
